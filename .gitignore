node_modules/
build/
dist/
coverage/
.DS_Store
*.log
.env*
!.env.example

# SQLite data store (host-side shared data)
data/

# Stray build/probe artifact (not part of the project)
_probe_index.html

# Local Claude Code settings (may contain secrets)
.claude/
